'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Login(){ const r=useRouter(); const [error,setError]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); const res=await fetch('/api/auth/login',{method:'POST',body:JSON.stringify({email:fd.get('email'),password:fd.get('password')})}); if(res.ok) r.push('/dashboard'); else setError('Invalid email or password');} return <form onSubmit={submit} className="card mx-auto max-w-md"><h1 className="text-2xl font-black">Login</h1>{error&&<p className="mt-3 text-red-600">{error}</p>}<label className="mt-4 block text-sm font-semibold">Email<input className="input mt-1" name="email" defaultValue="course@gualph.local"/></label><label className="mt-4 block text-sm font-semibold">Password<input className="input mt-1" name="password" type="password" defaultValue="Gualph123!"/></label><button className="btn mt-6 w-full">Login</button></form> }
